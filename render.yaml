services:
  - type: web
    name: pixelart-ai-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn backend_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
